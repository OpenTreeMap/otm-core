"use strict";

var map,
    inlineEditForm;

function init(options) {
    map = options.map;
    inlineEditForm = options.inlineEditForm;
}

function activate() {
}

function deactivate() {
}

module.exports = {
    init: init,
    activate: activate,
    deactivate: deactivate
};

